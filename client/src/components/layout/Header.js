import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      {/* Search box removed as per design correction */}
    </header>
  );
};

export default Header; 